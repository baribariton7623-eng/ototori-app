# 音取りアプリ MVP仕様書

## コンセプト
合唱団員・指揮者向けに、曲を選ぶとパートごとのメロディ音源(電子音)が
再生できる音取り練習ツール。将来的には合唱人口全体で使える公共ツールを
目指すが、MVPでは個人利用〜小規模運用を想定する。

## MVPスコープ

### 曲データ
- 運営者(自分)がMusicXMLファイルを事前に用意し、アプリに組み込む
- ユーザーによる曲の投稿機能は**MVPでは実装しない**
- MusicXML → アプリ内部の音符データ(パート名・音高・長さの配列)への
  変換パイプラインが必要(手動 or 半自動の変換スクリプト)

### パート構成
- 曲ごとに可変のパート数に対応する(2声、3声、4声(SATB)、分声など)
- データ構造は「パートの配列」とし、パート数を固定しない設計にする
- パートのラベル(ソプラノ/アルトI/アルトII等)は曲データ側で自由に定義できる

### 音符データ構造の要件(重要)
実際の合唱曲を表現するため、以下を必ず扱えるデータ設計にする:
- **休符**: 音のない区間を明示的なイベントとして持つ(例: `{ rest: true, beats: 1 }`)
- **音価は数値(拍数)で持つ**: `"4n"`のような音価文字列ではなく、
  四分音符=1.0とした拍数の数値で表現する(付点四分=1.5、三連八分=1/3、
  タイで結ばれた音=合算した拍数、として自然に表せる)
  例: `{ pitch: "C5", beats: 1.5 }`
- **弱起(アウフタクト)**: 曲頭が完全小節から始まらないケースに対応
  (小節位置の計算に影響するため、冒頭の不完全小節の拍数を曲データに持つ)
- **テンポ変化**: 楽章内のテンポ変更(例: レチタティーヴォ→合唱)や
  フェルマータ、リタルダンドを表現できるよう、テンポイベントの
  時系列リストを持てる構造にする(MVPでは「区間ごとのBPM指定」まで対応し、
  連続的なrit.は簡略化してよい)
- **繰り返し記号・ダカーポ**: MVPでは「展開済み(繰り返しを開いた状態)の
  データとして書き出す」方針とし、リピート構造の解釈はデータ生成時に解決する

### 作品と楽章の階層構造(重要)
収録候補がオラトリオ・受難曲・ミサ等の多楽章作品のため、
データとUIは以下の階層で設計する:
- **作品(Work)** > **楽章/ナンバー(Movement)** > **パート(Part)** > 音符列
- 曲選択UIも「作品を選ぶ → 楽章を選ぶ → パート画面」の2段階ナビゲーションとする
- 楽章ごとに独立した基準BPM・拍子・パート編成を持てるようにする
  (楽章によって合唱の編成が変わる作品があるため)

### アカウント機能
- ログインは**必須にしない**。曲選択・パート再生などコア機能は
  ログインなしで誰でも使える(認証のハードルで離脱させないため)
- ログインは「お気に入り・練習履歴を保存したい人向けの任意機能」として提供する
- ログインした場合に保存する対象(要検討・優先度低い順に):
  - お気に入り曲
  - 各パートのミュート設定(前回の状態を復元)
  - 練習した曲の履歴
- 認証方式: マジックリンク(メール)+ Googleログインの併用

### 再生機能(試作済み・流用可能)
- 曲(作品→楽章)を選択 → パート一覧が表示される
- 各パートを単独再生できる
- 複数パートをミュート/ミュート解除しながら同時再生できる
- **パートごとの音量バランス調整**: ミュートのON/OFFだけでなく、
  各パートに音量スライダーを持たせる(「自分のパートを大きく、
  他パートを小さく鳴らして合わせる」練習が音取りの定番のため)
- 再生中の音符をハイライト表示する
- テンポ(BPM)は曲データの基準値を初期値としつつ、ユーザーがスライダー等で
  リアルタイムに変更できる(例: 50%〜150%、または実数BPM入力)
  - 音取り練習では「ゆっくり確認→本来のテンポへ」という使い方を想定
  - 再生中のテンポ変更にも対応する(Tone.js の Transport.bpm で対応可能)
- **移動(シーク)は小節単位を基本とする**
  - テンポ可変のため「10秒スキップ」は意味が曖昧になる(50%テンポ時の
    10秒は楽譜上5秒分)。よって秒数ではなく楽譜上の位置で移動する
  - ダブルタップで「前後に◯小節スキップ」(例: 4小節)
  - シークバー(小節番号ベース)での任意位置ジャンプ
  - 内部的にはTone.js Transport の再生位置を拍/小節位置で移動する
- **A-Bリピート(区間反復)**: 苦手箇所の反復練習のため、開始小節と
  終了小節を指定してその区間をループ再生できるようにする(音取り練習の
  中核機能として MVP に含める)

### モバイル音声の制約への対応
- iOS Safari等はユーザー操作なしに音を出せないため、初回の再生操作
  (タップ)で `Tone.start()` を呼ぶ設計を徹底する
- 画面ロック・バックグラウンド移行で音が止まる制約があるため、
  再生中は画面スリープを抑止する(Screen Wake Lock API等)。
  対応できない環境では制約として明記する

### 非スコープ(MVPではやらない)
- ユーザーによる曲データ投稿・共有
- 広告表示
- 現代曲など著作権処理が必要な曲の取り扱い(当面はパブリックドメイン曲中心)
- 高度な採譜支援(PDF/画像からの自動採譜)
- **歌詞の表示・発音支援**: ラテン語(教会式/ドイツ式など発音流派の違い)や
  ドイツ語の発音指導は指導者の解釈に関わる領域であり、アプリが特定の
  発音を提示すると現場との齟齬を生むため、MVPでは音(音高・リズム)のみに
  専念する。歌詞は楽譜(手元の譜面)で確認してもらう前提とする

## データ設計の方向性
- 曲データはJSON形式(作品>楽章>パートの階層、パート可変長の配列構造)
- 認証・ユーザーデータ保存のバックエンドはSupabaseを使用
  (詳細は「バックエンド構成」参照)
- フロントエンドは既存の試作(React + Tone.js)をベースに拡張

## テンポ(BPM)の定義
- BPM = 1分間に鳴らす四分音符の数(一般的なメトロノーム表記と同じ、QPM)
  - 例: BPM=60 → 四分音符1つ=1秒、BPM=120 → 四分音符1つ=0.5秒
- 曲データの「基準BPM」もこの定義(四分音符=1拍換算)で統一する
- 注意: 曲によっては記譜上の1拍が四分音符でない場合がある
  (例: 8分音符を1拍とする曲、6/8などの複合拍子)。その場合は
  曲データ側に「何を1拍として基準BPMを設定したか」を明示する
  フィールドを持たせる(例: `beatUnit: "4n"` のような形)

## 配布形態
- ネイティブアプリ(App Store / Google Play)ではなく**Webアプリとして提供する**
  - ストア年会費・審査が不要で、URL共有だけで誰でも即使える
    (合唱団のグループLINE等に貼るだけで配布できる)
  - 更新も即時反映され、運営コスト・保守の手間が最小で済む
- 将来的に**PWA(Progressive Web App)化**を検討する
  - ホーム画面への追加でアプリのように起動できるようになる
  - 追加コストが小さいため、MVP後の早い段階での対応候補とする

## バックエンド構成
- ホスティング: Netlify(koikeyusuke.comと同じ運用基盤)
- DB/BaaS: Supabase(PostgreSQL、認証機能も一体型、無料枠あり)
- 認証: Supabase Auth(マジックリンク + Googleログイン)

## MusicXML → 内部データへの変換方法
- **元データはMusicXMLに一本化する**。MIDIはタイ・声部の区別・付点等の
  記譜情報が失われる劣化フォーマットのため使用しない。休符・タイ・弱起の
  正確な抽出にはMusicXMLが必須
1. MuseScore等でパートごとに音符を入力し、.musicxml として書き出す(手動・人手)
2. MusicXML解析ライブラリ(JS想定: `musicxml-interfaces`、`opensheetmusicdisplay`の
   パーサ部、またはPythonの `music21` 等から選定)でファイルを読み込み、
   パートごとの音高・音価・休符・タイ情報を自動抽出する
3. 変換スクリプト(一度作成すれば使い回し可能)で、抽出した情報を
   アプリの内部データ形式(拍数ベースの音符/休符イベント配列、パートごと)に整形する
- 新しい曲を追加する際は、①の楽譜入力以降(②③)は自動処理される

## MVP完成の定義
以下がすべて満たされた時点でMVP完成とする:
- 少なくとも1作品・1楽章分の曲データが収録され、全パートが正しく再生できる
- パート単独再生・複数パート同時再生・ミュート・音量バランスが動作する
- テンポ変更(再生中含む)・小節単位のシーク・A-Bリピートが動作する
- ログインなしで全コア機能が使える。任意ログインでお気に入り保存が動作する
- スマホ実機(iOS Safari / Android Chrome)で音が正しく鳴り、
  実用的な操作感であることを確認済み
- Netlifyにデプロイされ、URLでアクセスできる

## 最初に収録する曲(候補)
- ヘンデル: メサイア
- ベートーヴェン: 交響曲第九番(合唱部分)
- バッハ: マタイ受難曲
- バッハ: ヨハネ受難曲
- バッハ: ロ短調ミサ
- 注: 作曲者自体はパブリックドメインだが、使用する版(エディション)に
  出版社の権利が残っている場合があるため、IMSLP等で各版の著作権表記を
  個別に確認すること
- 上記は大曲のため、MVPでは全曲ではなく代表的な合唱曲(抜粋)から
  着手するのが現実的
